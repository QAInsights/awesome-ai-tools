---
/**
 * DashboardLayout - used by settings, help, zap, and compare pages.
 * Extends BaseLayout with the sidebar (tool variant = nav links, back button)
 * and the shared mobile header / desktop sidebar toggle chrome.
 *
 * Props:
 *   title, description - forwarded to BaseLayout
 *   activeNav - highlights the active link in the sidebar nav
 */
import BaseLayout from './BaseLayout.astro';
import Sidebar from '../components/Sidebar.astro';
import SiteHeader from '../components/SiteHeader.astro';

interface Props {
    title: string;
    description?: string;
    canonical?: string;
    activeNav?: 'settings' | 'zap' | 'help' | 'compare' | string;
    ogType?: string;
    ogImage?: string;
    jsonLd?: string;
    robots?: string;
}

const { title, description, canonical, activeNav, ogType, ogImage, jsonLd, robots } = Astro.props;
---

<BaseLayout title={title} description={description} canonical={canonical} ogType={ogType} ogImage={ogImage} jsonLd={jsonLd} robots={robots}>
    <div class="flex h-[100dvh] overflow-hidden">
        <Sidebar variant="tool" activeNav={activeNav} />

        <main class="flex-1 flex flex-col h-full bg-black min-w-0 relative overflow-y-auto">
            <SiteHeader variant="page" title={title.replace(' | ai.dosa.dev', '').replace('ai.dosa.dev | ', '')} />

            <slot />
            <footer class="shrink-0 mt-auto px-6 md:px-8 pt-10 pb-8 text-center font-mono text-[11px] text-ink-disabled">
                ai.dosa.dev · <a href="/blog" class="hover:text-ink-secondary transition-colors">Blog</a> · <a href="/news" class="hover:text-ink-secondary transition-colors">Today in AI</a>
            </footer>
        </main>
    </div>

    <script src="/dist/dashboard.js" type="module" defer></script>
</BaseLayout>
